import { Asistencia.Controller } from './asistencia.controller';

describe('Asistencia.Controller', () => {
  it('should create an instance', () => {
    expect(new Asistencia.Controller()).toBeTruthy();
  });
});

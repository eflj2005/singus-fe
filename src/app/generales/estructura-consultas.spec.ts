import { EstructuraConsultas } from './estructura-consultas';

describe('EstructuraConsultas', () => {
  it('should create an instance', () => {
    expect(new EstructuraConsultas()).toBeTruthy();
  });
});
